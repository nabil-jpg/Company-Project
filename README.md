# Company Project

1. Clone repo
2. Buat database (atau import setup.sql via phpMyAdmin)
3. Copy env.example -> .env dan sesuaikan (DB credentials)
4. Install dependency: pip install -r requirements.txt (atau pip install python-dotenv mysql-connector-python)
5. Jalankan example.py
